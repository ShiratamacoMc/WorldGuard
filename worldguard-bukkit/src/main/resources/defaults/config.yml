#
# WorldGuard's configuration file
#
# About editing this file:
# - DO NOT USE TABS. You MUST use spaces or Bukkit will complain. If
#   you use an editor like Notepad++ (recommended for Windows users), you
#   must configure it to "replace tabs with spaces." In Notepad++, this can
#   be changed in Settings > Preferences > Language Menu.
# - Don't get rid of the indents. They are indented so some entries are
#   in categories (like "enforce-single-session" is in the "protection"
#   category.
# - If you want to check the format of this file before putting it
#   into WorldGuard, paste it into http://yaml-online-parser.appspot.com/
#   and see if it gives "ERROR:".
# - Lines starting with # are commentsand so they are ignored.
#
# WARNING:
# Remember to check the compatibility spreadsheet for WorldGuard to see
# if any features are currently broken in your version of Bukkit.
#

# 世界白名单设置
# 如果启用，WorldGuard只会在白名单中的世界生效
# 在非白名单世界中，所有WorldGuard功能都会被禁用
world-whitelist:
    enabled: false
    worlds:
        - world
        - world_nether
        - world_the_end
    # Optional per-server override for the localized deny message (supports MiniMessage).
    # deny-message: "<red>This world is not managed by WorldGuard.</red>"

# -- This should be automatically replaced by the plugin in-game --
